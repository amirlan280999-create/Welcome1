balance = 10000

print("=== БАНКОМАТ ===")
print(f"Ваш текущий баланс: {balance} руб.")
print("Введите сумму для снятия или 'exit' для выхода")

while True:
    # Запрашиваем ввод пользователя
    user_input = input("\nСколько снять (или 'exit'): ").strip()

    # Проверяем на выход
    if user_input.lower() == "exit":
        print("Спасибо за использование банкомата! До свидания!")
        break

    # Пробуем обработать число
    try:
        amount = int(user_input)

        # Проверяем корректность суммы
        if amount <= 0:
            print("Ошибка: Сумма должна быть положительным числом!")
            continue

        # Проверяем наличие средств
        if amount > balance:
            print(f"Недостаточно средств! Ваш баланс: {balance} руб.")
        else:
            # Снимаем деньги
            balance -= amount
            print(f"✅ Успешно снято {amount} руб.")
            print(f"💰 Остаток на счете: {balance} руб.")

    except ValueError:
        # Если введено не число и не exit
        print(" Ошибка! Пожалуйста, введите число или 'exit'")

    # Проверяем нулевой баланс
    if balance == 0:
        print("\n️ На вашем счете закончились деньги!")
        print("Банкомат завершает работу.")
        break

print(f"Итоговый баланс: {balance} руб.")

